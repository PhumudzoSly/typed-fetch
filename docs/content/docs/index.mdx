---
title: Overview
description: typed-fetch - Privacy-first, status-aware fetch with automatic TypeScript types
icon: Home
---

import { Cards, Card } from 'fumadocs-ui/components/card';
import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Welcome to typed-fetch

<Callout type="info" title="What is typed-fetch?">
**Status-aware `fetch` wrapper that learns response shapes and generates TypeScript types from real traffic.**

typed-fetch eliminates manual type definitions for HTTP APIs while maintaining strict privacy. It observes your real network traffic, infers response shapes, and generates accurate TypeScript types—all without storing raw data.
</Callout>

## Why typed-fetch?

<Cards>
  <Card
    title="⚡ Automatic Type Generation"
    description="Learn response shapes from actual API calls and generate TypeScript definitions automatically. No manual typing required."
  />
  <Card
    title="🔒 Privacy First"
    description="Only structure is recorded, never raw response values. Strict privacy mode enabled by default protects sensitive data."
  />
  <Card
    title="📊 Status-Aware"
    description="Discriminated union types for HTTP status codes—type-safe error handling without try/catch blocks."
  />
  <Card
    title="🗄️ Built-in Cache"
    description="In-flight deduplication, stale-while-revalidate, GC, retry with back-off, and invalidation. Zero dependencies."
  />
  <Card
    title="⚛️ React Hooks"
    description="useTypedFetch, useTypedMutation, and TypedFetchProvider powered by useSyncExternalStore. Re-renders when cache updates."
  />
  <Card
    title="🔌 Server Adapters"
    description="Hono, Next.js App Router, and a generic adapter that auto-generate types from server-side responses in development."
  />
  <Card
    title="🌐 Works Everywhere"
    description="Seamless support for Node.js, browsers, and mixed client/server architectures with unified type generation."
  />
  <Card
    title="🚀 Never Throws"
    description="Network errors return result objects, never exceptions. Explicit error handling with full type safety."
  />
</Cards>

## Quick Start

<Tabs items={['npm', 'pnpm', 'yarn']}>
  <Tab value="npm">
    ```bash
    npm install @phumudzo/typed-fetch
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @phumudzo/typed-fetch
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @phumudzo/typed-fetch
    ```
  </Tab>
</Tabs>

<Callout type="warn">
Node.js 18+ is required.
</Callout>

### Basic Example

```ts
import { typedFetch } from '@phumudzo/typed-fetch';

const result = await typedFetch(
  'https://api.example.com/users/123',
  { method: 'GET' },
  { endpointKey: 'GET /users/:id' }
);

if (result.status === 200) {
  console.log(result.data); // ✨ fully typed after `typed-fetch generate`
}
```

### With Caching

```ts
import { typedFetch, createTypedFetchCache } from '@phumudzo/typed-fetch';

const cache = createTypedFetchCache({ staleTime: 60_000 });

// First call — hits the network
const r1 = await typedFetch('/users/1', undefined, {
  endpointKey: 'GET /users/:id',
  cache,
});

// Within staleTime — instant, no network request
const r2 = await typedFetch('/users/1', undefined, {
  endpointKey: 'GET /users/:id',
  cache,
});
```

### React Hooks

```tsx
import { createTypedFetchCache } from '@phumudzo/typed-fetch';
import { TypedFetchProvider, useTypedFetch } from '@phumudzo/typed-fetch/react';

const cache = createTypedFetchCache({ staleTime: 60_000 });

function App() {
  return (
    <TypedFetchProvider cache={cache}>
      <UserCard id={1} />
    </TypedFetchProvider>
  );
}

function UserCard({ id }: { id: number }) {
  const { result, isLoading, isError, error } = useTypedFetch(
    `/api/users/${id}`,
    undefined,
    { endpointKey: 'GET /api/users/:id' },
  );

  if (isLoading) return <p>Loading…</p>;
  if (isError)   return <p>Error: {error?.error.message}</p>;
  if (result?.status === 200) return <h1>{result.data.name}</h1>;
  return null;
}
```

### Server Adapters (auto type generation)

```ts
// Hono — one line covers all routes
import { typedFetchObserver } from '@phumudzo/typed-fetch/adapters/hono';
app.use('*', typedFetchObserver()); // dev-only, no-op in production

// Next.js App Router
import { withTypedFetchObserver } from '@phumudzo/typed-fetch/adapters/next';
export const GET = withTypedFetchObserver(
  'GET /api/users/:id',
  async (req) => Response.json(await getUser(req)),
);
```

### Generate Types

Run your app to collect observations, then:

```bash
npx typed-fetch generate
```

(With server adapters, types regenerate automatically on every request in dev.)

## Key Features Explained

### Never Throws Exceptions

Unlike native `fetch`, typed-fetch returns all errors as result objects:

```ts
const result = await typedFetch(...);

// No try-catch needed!
if (result.status === 0) {
  handleNetworkError(result.error);
} else if (!result.ok) {
  handleHTTPError(result.status, result.data);
} else {
  processData(result.data);
}
```

### Type-Safe Endpoint Keys

IDEs autocomplete all known endpoint keys from generated and user-declared types:

```ts
await typedFetch(url, undefined, {
  endpointKey: "G|",
  //            ^-- IDE suggests "GET /users/:id", "GET /posts", …
});
```

### Status-Based Type Narrowing

```ts
const result = await typedFetch('/users/1', undefined, {
  endpointKey: 'GET /users/:id',
});

if (result.status === 200) {
  result.data.name;  // ✅ typed as string
}
if (result.status === 404) {
  result.data.error; // ✅ typed as string
}
```

## Next Steps

<Cards>
  <Card
    title="📦 Installation & Setup"
    description="Get started with installation and configuration"
    href="/docs/installation"
  />
  <Card
    title="📘 Basic Usage"
    description="Learn how to use typedFetch and generate types"
    href="/docs/basic-usage"
  />
  <Card
    title="🗝️ Endpoint Keys"
    description="Type-safe keys with autocomplete and manual declarations"
    href="/docs/endpoint-keys"
  />
  <Card
    title="🗄️ Caching"
    description="In-memory cache with deduplication, stale-while-revalidate, and retry"
    href="/docs/caching"
  />
  <Card
    title="⚛️ React Hooks"
    description="useTypedFetch, useTypedMutation, TypedFetchProvider"
    href="/docs/react-hooks"
  />
  <Card
    title="🔌 Adapters"
    description="Server-side type generation for Hono, Next.js, and more"
    href="/docs/adapters"
  />
</Cards>
